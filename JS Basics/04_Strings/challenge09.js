/*
    Form a Simple Sentence
    Given three string variables subject, verb, and object, concatenate them together with spaces to form a sentence.
    Store the result in a variable called sentence.

    const subject = 'The cat';
    const verb = 'chased';
    const object = 'the mouse';
    
    // your code here...
    
    console.log(sentence);
*/

const subject = 'The cat';
const verb = 'chased';
const object = 'the mouse';
 
// your code here...
const sentence = subject + ' ' + verb + ' ' + object;
 
console.log(sentence);