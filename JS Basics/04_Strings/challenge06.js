/*
    Find the Length of a String
    Use the .length property to find the number of characters in the given string message and assign the length to the variable len.
    
    const message = "JavaScript";
    const len = 
    console.log(len);
*/

const message = "JavaScript";
const len = message.length;
console.log(len);
