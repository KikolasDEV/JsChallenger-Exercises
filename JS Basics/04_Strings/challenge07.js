/*
    Repeat a Word Twice
    Given a string, concatenate it with itself using the + operator and store the result in a variable named doubled.
    
    const word = 'hi';
    
    // create variable 'double' here:
    
    console.log(doubled); // expected: "hihi"
*/

const word = 'hi';
 
// create variable 'double' here:
const doubled = word + word;
 
console.log(doubled); // expected: "hihi"