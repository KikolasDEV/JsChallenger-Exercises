/*
    Create an array with mixed types
    Create a new array called 'mixedArr' that contains at least one string, one number, and one boolean value (in any order).

    // Create a new array with mixed types
    const mixedArr = 
    
    // Log your array:
    console.log(mixedArr);
*/

const mixedArr = ['hello', 7, true];
console.log(mixedArr);