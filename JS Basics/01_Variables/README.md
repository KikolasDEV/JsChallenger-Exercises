# 01 — Variables (JS Basics)

Bloque centrado en declaración, asignación y acceso a variables en JavaScript.  
Objetivo: eliminar errores básicos y ganar disciplina de escritura (claridad > trucos).

---

## ✅ Qué aprendí / reforcé
- Cómo imprimir en consola y validar ejecución (`console.log`).
- Diferencia real entre **declarar** una variable y **asignarle** un valor.
- Cuándo usar **`let`** y cuándo usar **`const`**.
- Qué errores aparecen cuando:
  - declaras dos veces una variable,
  - intentas reasignar un `const`,
  - usas una variable mal escrita (`Num` vs `num`),
  - o usas una variable antes de declararla.
- Uso práctico de comentarios para habilitar/deshabilitar líneas de código.

---

## 🧠 Ideas clave
- Usa **`const`** por defecto y cambia a let solo si necesitas reasignar.
- **`const`** significa **no reasignable**, no “inmutable”.
- Nombres correctos y consistentes: si fallas en eso, fallas en todo lo demás.
- Comentarios no son decoración: sirven para controlar ejecución durante pruebas.